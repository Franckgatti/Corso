#include<stdio.h>
#include<math.h>

int main (void) {
    printf("inserisci la lunghezza del lato del quadrato di cui calcolare l'area:\n");
    int lato;
    scanf("%d", &lato);
    printf("il numero inserito è: %d",lato);
    int area = lato*lato;
    printf("l'area è:")





}
